version: "3"

services:
    auditorium:
        image: docker.pkg.github.com/apiad/auditorium/auditorium:latest
        build: "."
        volumes:
            - "./:/code"
        network_mode: host
        command: auditorium demo
        working_dir: "/"
